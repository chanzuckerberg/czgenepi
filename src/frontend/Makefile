### CHECK STYLE #############################################
check-style:
	npm --prefix . run lint-ci

lint:
	npm --prefix . run lint

### TESTING #############################################
test: # run ts test
	npm --prefix . test

test-build:
	npm --prefix . run build
